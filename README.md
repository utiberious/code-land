# Code Land

OpenCode 生成的代码项目集合。

## 结构

```
code-land/
└── mimi/           # 米米生成的项目
    ├── counter/    # React Counter 组件
    ├── todo-app/   # Todo App
    └── ...
```

## 使用方式

每个子目录是一个独立的项目，由 OpenCode 自动生成和提交。
